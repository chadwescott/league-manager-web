import { CardPanelContentDirective } from './card-panel-content.directive';

describe('CardPanelContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CardPanelContentDirective();
    expect(directive).toBeTruthy();
  });
});
