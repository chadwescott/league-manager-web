import { CardPanelTitleDirective } from './card-panel-title.directive';

describe('CardPanelTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new CardPanelTitleDirective();
    expect(directive).toBeTruthy();
  });
});
