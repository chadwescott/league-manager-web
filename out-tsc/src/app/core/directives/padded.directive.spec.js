import { PaddedDirective } from './padded.directive';
describe('PaddedDirective', () => {
    it('should create an instance', () => {
        const directive = new PaddedDirective();
        expect(directive).toBeTruthy();
    });
});
//# sourceMappingURL=padded.directive.spec.js.map